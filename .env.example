# Copy this file to `.env` and fill in your key. `.env` is gitignored.

# Anthropic Claude API key (Step 5). Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: override the model from config.yaml without editing it.
# LEGALCOMPARE_MODEL=claude-sonnet-4-6
